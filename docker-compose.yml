services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./logs:/app/logs
      - ./instance:/app/instance
    environment:
      - DATABASE_URL=sqlite:///instance/taskmaster.db
